name: fitness_app_prototype
description: "LeanIt Fitness adaptive training prototype."
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.17.1
  shared_preferences: ^2.5.3
  http: ^1.6.0
  cached_network_image: 4.0.0
  image_picker: ^1.2.0
  path_provider: ^2.1.5
  geolocator: ^14.0.2
  flutter_local_notifications: ^22.3.0
  flutter_timezone: ^5.1.0
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/exercises/
